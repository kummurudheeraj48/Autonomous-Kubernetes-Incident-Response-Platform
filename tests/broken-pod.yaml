apiVersion: v1
kind: Pod
metadata:
  name: test-fault-pod
  namespace: default
  labels:
    tier: testing
spec:
  containers:
  - name: broken-app
    image: nginx:this-tag-does-not-exist-on-registry
    imagePullPolicy: Always
